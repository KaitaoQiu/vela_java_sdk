openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    kustomize-spec:
      properties:
        branch:
          default: master
          description: "The Git reference to checkout and monitor for changes, defaults\
            \ to master branch."
          title: branch
          type: string
        path:
          description: "Path to the directory containing the kustomization.yaml file,\
            \ or the set of plain YAMLs a kustomization.yaml should be generated for."
          title: path
          type: string
        pullInterval:
          default: 1m
          description: The interval at which to check for repository updates.
          title: pullInterval
          type: string
        repoUrl:
          description: "The repository URL, can be a HTTP/S or SSH address."
          title: repoUrl
          type: string
      required:
      - branch
      - path
      - pullInterval
      - repoUrl
      title: parameter
      type: object


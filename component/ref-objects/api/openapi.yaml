openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    K8sObject:
      properties:
        cluster:
          description: "If specified, fetch the Kubernetes objects from the cluster.\
            \ Otherwise, fetch from the local cluster."
          title: cluster
          type: string
        group:
          description: The group name for the Kubernetes objects
          title: group
          type: string
        labelSelector:
          additionalProperties:
            type: string
          description: "If specified, fetch the Kubernetes objects according to the\
            \ label selector, exclusive to name"
          title: labelSelector
          type: object
        name:
          description: "If specified, fetch the Kubernetes objects with the name,\
            \ exclusive to labelSelector"
          title: name
          type: string
        namespace:
          description: "If specified, fetch the Kubernetes objects from the namespace.\
            \ Otherwise, fetch from the application's namespace."
          title: namespace
          type: string
        resource:
          description: The resource type for the Kubernetes objects
          title: resource
          type: string
      title: objects
      type: object
    ref-objects-spec:
      properties:
        objects:
          description: "If specified, application will fetch native Kubernetes objects\
            \ according to the object description"
          items:
            $ref: '#/components/schemas/K8sObject'
          title: objects
          type: array
        urls:
          description: "If specified, the objects in the urls will be loaded."
          items:
            title: urls
            type: string
          title: urls
          type: array
      title: parameter
      type: object


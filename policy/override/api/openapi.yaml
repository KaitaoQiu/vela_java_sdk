openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    PatchParams:
      properties:
        name:
          description: "Specify the name of the patch component, if empty, all components\
            \ will be merged"
          title: name
          type: string
        properties:
          description: Specify the properties to override.
          title: properties
          type: object
        traits:
          description: Specify the traits to override.
          items:
            $ref: '#/components/schemas/traits'
          title: traits
          type: array
        type:
          description: Specify the type of the patch component.
          title: type
          type: string
      title: components
      type: object
    override-spec:
      properties:
        components:
          description: Specify the overridden component configuration.
          items:
            $ref: '#/components/schemas/PatchParams'
          title: components
          type: array
        selector:
          description: "Specify a list of component names to use, if empty, all components\
            \ will be selected."
          items:
            title: selector
            type: string
          title: selector
          type: array
      required:
      - components
      title: parameter
      type: object
    traits:
      properties:
        disable:
          default: false
          description: "Specify if the trait should be remove, default false"
          title: disable
          type: boolean
        properties:
          description: Specify the properties to override.
          title: properties
          type: object
        type:
          description: Specify the type of the trait to be patched.
          title: type
          type: string
      required:
      - disable
      - type
      title: traits
      type: object


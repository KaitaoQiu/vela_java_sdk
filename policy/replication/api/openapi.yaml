openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    replication-spec:
      properties:
        keys:
          description: Spicify the keys of replication. Every key coresponds to a
            replication components
          items:
            title: keys
            type: string
          title: keys
          type: array
        selector:
          description: Specify the components which will be replicated.
          items:
            title: selector
            type: string
          title: selector
          type: array
      required:
      - keys
      title: parameter
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    topology-spec:
      properties:
        allowEmpty:
          description: Ignore empty cluster error
          title: allowEmpty
          type: boolean
        clusterLabelSelector:
          additionalProperties:
            type: string
          description: Specify the label selector for clusters
          title: clusterLabelSelector
          type: object
        clusterSelector:
          additionalProperties:
            type: string
          description: "Deprecated: Use clusterLabelSelector instead."
          title: clusterSelector
          type: object
        clusters:
          description: Specify the names of the clusters to select.
          items:
            title: clusters
            type: string
          title: clusters
          type: array
        namespace:
          description: "Specify the target namespace to deploy in the selected clusters,\
            \ default inherit the original namespace."
          title: namespace
          type: string
      title: parameter
      type: object


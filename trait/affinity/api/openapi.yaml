openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    affinity-spec:
      properties:
        nodeAffinity:
          $ref: '#/components/schemas/nodeAffinity'
        podAffinity:
          $ref: '#/components/schemas/podAffinity'
        podAntiAffinity:
          $ref: '#/components/schemas/podAntiAffinity'
        tolerations:
          description: Specify tolerant taint
          items:
            $ref: '#/components/schemas/tolerations'
          title: tolerations
          type: array
      title: parameter
      type: object
    labelSelector:
      properties:
        matchExpressions:
          items:
            $ref: '#/components/schemas/matchExpressions'
          title: matchExpressions
          type: array
        matchLabels:
          additionalProperties:
            type: string
          title: matchLabels
          type: object
      title: labelSelector
      type: object
    nodeSelecor:
      properties:
        key:
          title: key
          type: string
        operator:
          default: In
          enum:
          - In
          - NotIn
          - Exists
          - DoesNotExist
          - Gt
          - Lt
          title: operator
          type: string
        values:
          items:
            title: values
            type: string
          title: values
          type: array
      required:
      - key
      - operator
      title: matchFields
      type: object
    nodeSelectorTerm:
      properties:
        matchExpressions:
          items:
            $ref: '#/components/schemas/nodeSelecor'
          title: matchExpressions
          type: array
        matchFields:
          items:
            $ref: '#/components/schemas/nodeSelecor'
          title: matchFields
          type: array
      title: preference
      type: object
    podAffinityTerm:
      properties:
        labelSelector:
          $ref: '#/components/schemas/labelSelector'
        namespaceSelector:
          $ref: '#/components/schemas/labelSelector'
        namespaces:
          items:
            title: namespaces
            type: string
          title: namespaces
          type: array
        topologyKey:
          title: topologyKey
          type: string
      required:
      - topologyKey
      title: podAffinityTerm
      type: object
    preferred:
      properties:
        preference:
          $ref: '#/components/schemas/nodeSelectorTerm'
        weight:
          description: Specify weight associated with matching the corresponding nodeSelector
          maximum: 100
          minimum: 1
          title: weight
          type: integer
      required:
      - preference
      - weight
      title: preferred
      type: object
    required:
      description: Specify the required during scheduling ignored during execution
      properties:
        nodeSelectorTerms:
          description: Specify a list of node selector
          items:
            $ref: '#/components/schemas/nodeSelectorTerm'
          title: nodeSelectorTerms
          type: array
      required:
      - nodeSelectorTerms
      title: required
      type: object
    nodeAffinity:
      description: Specify the node affinity scheduling rules for the pod
      properties:
        preferred:
          description: Specify the preferred during scheduling ignored during execution
          items:
            $ref: '#/components/schemas/preferred'
          title: preferred
          type: array
        required:
          $ref: '#/components/schemas/required'
      title: nodeAffinity
      type: object
    preferred_1:
      properties:
        podAffinityTerm:
          $ref: '#/components/schemas/podAffinityTerm'
        weight:
          description: Specify weight associated with matching the corresponding podAffinityTerm
          maximum: 100
          minimum: 1
          title: weight
          type: integer
      required:
      - podAffinityTerm
      - weight
      title: preferred
      type: object
    podAffinity:
      description: Specify the pod affinity scheduling rules
      properties:
        preferred:
          description: Specify the preferred during scheduling ignored during execution
          items:
            $ref: '#/components/schemas/preferred_1'
          title: preferred
          type: array
        required:
          description: Specify the required during scheduling ignored during execution
          items:
            $ref: '#/components/schemas/podAffinityTerm'
          title: required
          type: array
      title: podAffinity
      type: object
    podAntiAffinity:
      description: Specify the pod anti-affinity scheduling rules
      properties:
        preferred:
          description: Specify the preferred during scheduling ignored during execution
          items:
            $ref: '#/components/schemas/preferred_1'
          title: preferred
          type: array
        required:
          description: Specify the required during scheduling ignored during execution
          items:
            $ref: '#/components/schemas/podAffinityTerm'
          title: required
          type: array
      title: podAntiAffinity
      type: object
    tolerations:
      properties:
        effect:
          enum:
          - NoSchedule
          - PreferNoSchedule
          - NoExecute
          title: effect
          type: string
        key:
          title: key
          type: string
        operator:
          default: Equal
          enum:
          - Equal
          - Exists
          title: operator
          type: string
        tolerationSeconds:
          description: Specify the period of time the toleration
          title: tolerationSeconds
          type: integer
        value:
          title: value
          type: string
      required:
      - operator
      title: tolerations
      type: object
    matchExpressions:
      properties:
        key:
          title: key
          type: string
        operator:
          default: In
          enum:
          - In
          - NotIn
          - Exists
          - DoesNotExist
          title: operator
          type: string
        values:
          items:
            title: values
            type: string
          title: values
          type: array
      required:
      - key
      - operator
      title: matchExpressions
      type: object


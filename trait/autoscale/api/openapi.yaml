openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    autoscale-spec:
      properties:
        cpuPercent:
          description: |-
            Specify the value for CPU utilization, like 80, which means 80%
            +alias=cpu-percent
          title: cpuPercent
          type: integer
        cron:
          $ref: '#/components/schemas/cron'
        max:
          description: Maximal replicas of the workload
          title: max
          type: integer
        min:
          description: Minimal replicas of the workload
          title: min
          type: integer
      required:
      - max
      - min
      title: parameter
      type: object
    cron:
      description: "Cron type auto-scaling. Just for `appfile`, not available for\
        \ Cli usage"
      properties:
        days:
          description: "Several workdays or weekends, like \"Monday, Tuesday\""
          title: days
          type: string
        duration:
          description: For how long the scaling will last
          title: duration
          type: string
        replicas:
          description: The target replicas to be scaled to
          title: replicas
          type: integer
        startAt:
          description: "The time to start scaling, like `08:00`"
          title: startAt
          type: string
        timezone:
          description: "Timezone, like \"America/Los_Angeles\""
          title: timezone
          type: string
      required:
      - days
      - duration
      - replicas
      - startAt
      - timezone
      title: cron
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    PatchParams:
      properties:
        addArgs:
          description: "Specify the args to add in the target container, existing\
            \ args will be kept, cannot be used with `args`"
          items:
            title: addArgs
            type: string
          nullable: true
          title: addArgs
          type: array
        args:
          description: "Specify the args to use in the target container, if set, it\
            \ will override existing args"
          items:
            title: args
            type: string
          nullable: true
          title: args
          type: array
        command:
          description: "Specify the command to use in the target container, if not\
            \ set, it will not be changed"
          items:
            title: command
            type: string
          nullable: true
          title: command
          type: array
        containerName:
          default: ""
          description: "Specify the name of the target container, if not set, use\
            \ the component name"
          title: containerName
          type: string
        delArgs:
          description: "Specify the existing args to delete in the target container,\
            \ cannot be used with `args`"
          items:
            title: delArgs
            type: string
          nullable: true
          title: delArgs
          type: array
      required:
      - addArgs
      - args
      - command
      - containerName
      - delArgs
      title: containers
      type: object
    command-spec:
      oneOf:
      - $ref: '#/components/schemas/PatchParams'
      - $ref: '#/components/schemas/command_spec_oneOf'
      title: parameter
      type: object
    command_spec_oneOf:
      properties:
        containers:
          description: Specify the commands for multiple containers
          items:
            $ref: '#/components/schemas/PatchParams'
          title: containers
          type: array
      required:
      - containers
      type: object
      example: null


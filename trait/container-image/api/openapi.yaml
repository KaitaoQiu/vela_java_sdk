openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    PatchParams:
      properties:
        containerName:
          default: ""
          description: "Specify the name of the target container, if not set, use\
            \ the component name"
          title: containerName
          type: string
        image:
          description: Specify the image of the container
          title: image
          type: string
        imagePullPolicy:
          default: ""
          description: Specify the image pull policy of the container
          enum:
          - ""
          - IfNotPresent
          - Always
          - Never
          title: imagePullPolicy
          type: string
      required:
      - containerName
      - image
      - imagePullPolicy
      title: containers
      type: object
    container-image-spec:
      oneOf:
      - $ref: '#/components/schemas/PatchParams'
      - $ref: '#/components/schemas/container_image_spec_oneOf'
      title: parameter
      type: object
    container_image_spec_oneOf:
      properties:
        containers:
          description: Specify the container image for multiple containers
          items:
            $ref: '#/components/schemas/PatchParams'
          title: containers
          type: array
      required:
      - containers
      type: object
      example: null


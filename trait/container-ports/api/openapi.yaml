openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    PatchParams:
      properties:
        containerName:
          default: ""
          description: "Specify the name of the target container, if not set, use\
            \ the component name"
          title: containerName
          type: string
        ports:
          description: Specify ports you want customer traffic sent to
          items:
            $ref: '#/components/schemas/ports'
          title: ports
          type: array
      required:
      - containerName
      - ports
      title: containers
      type: object
    container-ports-spec:
      oneOf:
      - $ref: '#/components/schemas/PatchParams'
      - $ref: '#/components/schemas/container_ports_spec_oneOf'
      title: parameter
      type: object
    ports:
      properties:
        containerPort:
          description: Number of port to expose on the pod's IP address
          title: containerPort
          type: integer
        hostIP:
          description: What host IP to bind the external port to.
          title: hostIP
          type: string
        hostPort:
          description: Number of port to expose on the host
          title: hostPort
          type: integer
        protocol:
          default: TCP
          description: "Protocol for port. Must be UDP, TCP, or SCTP"
          enum:
          - TCP
          - UDP
          - SCTP
          title: protocol
          type: string
      required:
      - containerPort
      - protocol
      title: ports
      type: object
    container_ports_spec_oneOf:
      properties:
        containers:
          description: Specify the container ports for multiple containers
          items:
            $ref: '#/components/schemas/PatchParams'
          title: containers
          type: array
      required:
      - containers
      type: object
      example: null


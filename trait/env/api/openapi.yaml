openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    PatchParams:
      properties:
        containerName:
          default: ""
          description: "Specify the name of the target container, if not set, use\
            \ the component name"
          title: containerName
          type: string
        env:
          additionalProperties:
            type: string
          description: "Specify the  environment variables to merge, if key already\
            \ existing, override its value"
          title: env
          type: object
        replace:
          default: false
          description: Specify if replacing the whole environment settings for the
            container
          title: replace
          type: boolean
        unset:
          description: Specify which existing environment variables to unset
          items:
            title: unset
            type: string
          title: unset
          type: array
      required:
      - containerName
      - env
      - replace
      - unset
      title: containers
      type: object
    env-spec:
      oneOf:
      - $ref: '#/components/schemas/PatchParams'
      - $ref: '#/components/schemas/env_spec_oneOf'
      title: parameter
      type: object
    env_spec_oneOf:
      properties:
        containers:
          description: Specify the environment variables for multiple containers
          items:
            $ref: '#/components/schemas/PatchParams'
          title: containers
          type: array
      required:
      - containers
      type: object
      example: null


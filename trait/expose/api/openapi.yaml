openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    expose-spec:
      properties:
        domain:
          title: domain
          type: string
        http:
          additionalProperties:
            type: integer
          title: http
          type: object
      required:
      - domain
      - http
      title: parameter
      type: object


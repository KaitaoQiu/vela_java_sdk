openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    hostalias-spec:
      properties:
        hostAliases:
          description: Specify the hostAliases to add
          items:
            $ref: '#/components/schemas/hostAliases'
          title: hostAliases
          type: array
      required:
      - hostAliases
      title: parameter
      type: object
    hostAliases:
      properties:
        hostnames:
          items:
            title: hostnames
            type: string
          title: hostnames
          type: array
        ip:
          title: ip
          type: string
      required:
      - hostnames
      - ip
      title: hostAliases
      type: object


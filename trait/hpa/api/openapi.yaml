openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    hpa-spec:
      properties:
        cpuUtil:
          default: 50
          title: cpuUtil
          type: integer
        max:
          default: 10
          title: max
          type: integer
        min:
          default: 1
          title: min
          type: integer
      required:
      - cpuUtil
      - max
      - min
      title: parameter
      type: object


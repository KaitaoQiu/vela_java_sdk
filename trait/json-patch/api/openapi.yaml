openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    json-patch-spec:
      properties:
        operations:
          items:
            title: operations
            type: object
          title: operations
          type: array
      required:
      - operations
      title: parameter
      type: object


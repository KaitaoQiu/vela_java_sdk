openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    kautoscale-spec:
      properties:
        max:
          default: 3
          title: max
          type: integer
        min:
          default: 1
          title: min
          type: integer
      required:
      - max
      - min
      title: parameter
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    metrics-spec:
      properties:
        enabled:
          default: true
          title: enabled
          type: boolean
        format:
          default: prometheus
          description: |-
            Format of the metrics, default as prometheus
            +short=f
          title: format
          type: string
        path:
          default: /metrics
          description: The metrics path of the service
          title: path
          type: string
        port:
          $ref: '#/components/schemas/port'
        scheme:
          default: http
          description: The way to retrieve data which can take the values `http` or
            `https`
          title: scheme
          type: string
        selector:
          additionalProperties:
            type: string
          description: "The label selector for the pods, will discovery automatically\
            \ by default"
          title: selector
          type: object
      required:
      - enabled
      - format
      - path
      - port
      - scheme
      title: parameter
      type: object
    port:
      description: "The port for metrics, will discovery automatically by default"
      oneOf:
      - enum:
        - 0
        type: integer
      title: port
      type: integer


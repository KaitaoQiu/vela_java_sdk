openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    nocalhost-spec:
      properties:
        command:
          $ref: '#/components/schemas/command'
        debug:
          $ref: '#/components/schemas/debug'
        env:
          items:
            $ref: '#/components/schemas/env'
          title: env
          type: array
        gitUrl:
          title: gitUrl
          type: string
        hotReload:
          default: true
          title: hotReload
          type: boolean
        image:
          $ref: '#/components/schemas/image'
        persistentVolumeDirs:
          items:
            $ref: '#/components/schemas/persistentVolumeDirs'
          title: persistentVolumeDirs
          type: array
        port:
          title: port
          type: integer
        portForward:
          items:
            title: portForward
            type: string
          title: portForward
          type: array
        resources:
          $ref: '#/components/schemas/resources'
        serviceType:
          default: deployment
          title: serviceType
          type: string
        shell:
          default: bash
          title: shell
          type: string
        storageClass:
          title: storageClass
          type: string
        sync:
          $ref: '#/components/schemas/sync'
        workDir:
          default: /home/nocalhost-dev
          title: workDir
          type: string
      required:
      - command
      - hotReload
      - image
      - port
      - resources
      - serviceType
      - shell
      - sync
      - workDir
      title: parameter
      type: object
    command:
      properties:
        debug:
          default:
          - sh
          - debug.sh
          items:
            title: debug
            type: string
          title: debug
          type: array
        run:
          default:
          - sh
          - run.sh
          items:
            title: run
            type: string
          title: run
          type: array
      required:
      - debug
      - run
      title: command
      type: object
    debug:
      properties:
        remoteDebugPort:
          title: remoteDebugPort
          type: integer
      title: debug
      type: object
    env:
      properties:
        name:
          title: name
          type: string
        value:
          title: value
          type: string
      required:
      - name
      - value
      title: env
      type: object
    image:
      oneOf:
      - enum:
        - go
        - java
        - python
        - node
        - ruby
        type: string
      title: image
      type: string
    persistentVolumeDirs:
      properties:
        capacity:
          title: capacity
          type: string
        path:
          title: path
          type: string
      required:
      - capacity
      - path
      title: persistentVolumeDirs
      type: object
    limits:
      properties:
        cpu:
          default: "2"
          title: cpu
          type: string
        memory:
          default: 2Gi
          title: memory
          type: string
      required:
      - cpu
      - memory
      title: limits
      type: object
    requests:
      properties:
        cpu:
          default: "0.5"
          title: cpu
          type: string
        memory:
          default: 512Mi
          title: memory
          type: string
      required:
      - cpu
      - memory
      title: requests
      type: object
    resources:
      properties:
        limits:
          $ref: '#/components/schemas/limits'
        requests:
          $ref: '#/components/schemas/requests'
      required:
      - limits
      - requests
      title: resources
      type: object
    sync:
      properties:
        filePattern:
          default:
          - ./
          items:
            title: filePattern
            type: string
          title: filePattern
          type: array
        ignoreFilePattern:
          default:
          - .git
          - .vscode
          - .idea
          - .gradle
          - build
          items:
            title: ignoreFilePattern
            type: string
          title: ignoreFilePattern
          type: array
        type:
          default: send
          title: type
          type: string
      required:
      - filePattern
      - ignoreFilePattern
      - type
      title: sync
      type: object


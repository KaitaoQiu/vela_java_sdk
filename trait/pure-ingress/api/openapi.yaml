openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    pure-ingress-spec:
      properties:
        domain:
          description: Specify the domain you want to expose
          title: domain
          type: string
        http:
          additionalProperties:
            type: integer
          description: Specify the mapping relationship between the http path and
            the workload port
          title: http
          type: object
      required:
      - domain
      - http
      title: parameter
      type: object


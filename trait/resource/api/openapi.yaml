openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    resource-spec:
      properties:
        cpu:
          $ref: '#/components/schemas/cpu'
        limits:
          $ref: '#/components/schemas/limits'
        memory:
          default: 2048Mi
          description: Specify the amount of memory for requests and limits
          pattern: "^([1-9][0-9]{0,63})(E|P|T|G|M|K|Ei|Pi|Ti|Gi|Mi|Ki)$"
          title: memory
          type: string
        requests:
          $ref: '#/components/schemas/requests'
      title: parameter
      type: object
    cpu:
      description: Specify the amount of cpu for requests and limits
      oneOf:
      - default: 1
        type: number
      - type: string
      title: cpu
    cpu_1:
      description: Specify the amount of cpu for limits
      oneOf:
      - default: 1
        type: number
      - type: string
      title: cpu
    limits:
      description: Specify the resources in limits
      properties:
        cpu:
          $ref: '#/components/schemas/cpu_1'
        memory:
          default: 2048Mi
          description: Specify the amount of memory for limits
          pattern: "^([1-9][0-9]{0,63})(E|P|T|G|M|K|Ei|Pi|Ti|Gi|Mi|Ki)$"
          title: memory
          type: string
      required:
      - cpu
      - memory
      title: limits
      type: object
    cpu_2:
      description: Specify the amount of cpu for requests
      oneOf:
      - default: 1
        type: number
      - type: string
      title: cpu
    requests:
      description: Specify the resources in requests
      properties:
        cpu:
          $ref: '#/components/schemas/cpu_2'
        memory:
          default: 2048Mi
          description: Specify the amount of memory for requests
          pattern: "^([1-9][0-9]{0,63})(E|P|T|G|M|K|Ei|Pi|Ti|Gi|Mi|Ki)$"
          title: memory
          type: string
      required:
      - cpu
      - memory
      title: requests
      type: object


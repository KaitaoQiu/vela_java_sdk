openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    route-spec:
      properties:
        domain:
          default: ""
          description: Domain name
          title: domain
          type: string
        ingressClass:
          title: ingressClass
          type: string
        issuer:
          default: ""
          title: issuer
          type: string
        provider:
          title: provider
          type: string
        rules:
          items:
            $ref: '#/components/schemas/rules'
          title: rules
          type: array
      required:
      - domain
      - issuer
      title: parameter
      type: object
    rules:
      properties:
        path:
          title: path
          type: string
        rewriteTarget:
          default: ""
          title: rewriteTarget
          type: string
      required:
      - path
      - rewriteTarget
      title: rules
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    scaler-spec:
      properties:
        replicas:
          default: 1
          description: Specify the number of workload
          title: replicas
          type: integer
      required:
      - replicas
      title: parameter
      type: object


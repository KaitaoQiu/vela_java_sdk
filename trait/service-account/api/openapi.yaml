openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    Privileges:
      properties:
        apiGroups:
          description: Specify the apiGroups of the resource
          items:
            title: apiGroups
            type: string
          title: apiGroups
          type: array
        nonResourceURLs:
          description: Specify the resource url to be allowed
          items:
            title: nonResourceURLs
            type: string
          title: nonResourceURLs
          type: array
        resourceNames:
          description: Specify the resourceNames to be allowed
          items:
            title: resourceNames
            type: string
          title: resourceNames
          type: array
        resources:
          description: Specify the resources to be allowed
          items:
            title: resources
            type: string
          title: resources
          type: array
        scope:
          default: namespace
          description: "Specify the scope of the privileges, default to be namespace\
            \ scope"
          enum:
          - namespace
          - cluster
          title: scope
          type: string
        verbs:
          description: Specify the verbs to be allowed for the resource
          items:
            title: verbs
            type: string
          title: verbs
          type: array
      required:
      - scope
      - verbs
      title: privileges
      type: object
    service-account-spec:
      properties:
        create:
          default: false
          description: Specify whether to create new ServiceAccount or not
          title: create
          type: boolean
        name:
          description: Specify the name of ServiceAccount
          title: name
          type: string
        privileges:
          description: "Specify the privileges of the ServiceAccount, if not empty,\
            \ RoleBindings(ClusterRoleBindings) will be created"
          items:
            $ref: '#/components/schemas/Privileges'
          title: privileges
          type: array
      required:
      - create
      - name
      title: parameter
      type: object


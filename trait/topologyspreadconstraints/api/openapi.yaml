openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    labSelector:
      properties:
        matchExpressions:
          items:
            $ref: '#/components/schemas/matchExpressions'
          title: matchExpressions
          type: array
        matchLabels:
          additionalProperties:
            type: string
          title: matchLabels
          type: object
      title: labelSelector
      type: object
    topologyspreadconstraints-spec:
      properties:
        constraints:
          items:
            $ref: '#/components/schemas/constraints'
          title: constraints
          type: array
      required:
      - constraints
      title: parameter
      type: object
    matchExpressions:
      properties:
        key:
          title: key
          type: string
        operator:
          default: In
          enum:
          - In
          - NotIn
          - Exists
          - DoesNotExist
          title: operator
          type: string
        values:
          items:
            title: values
            type: string
          title: values
          type: array
      required:
      - key
      - operator
      title: matchExpressions
      type: object
    constraints:
      properties:
        labelSelector:
          $ref: '#/components/schemas/labSelector'
        matchLabelKeys:
          description: A list of pod label keys to select the pods over which spreading
            will be calculated
          items:
            title: matchLabelKeys
            type: string
          title: matchLabelKeys
          type: array
        maxSkew:
          description: Describe the degree to which Pods may be unevenly distributed
          title: maxSkew
          type: integer
        minDomains:
          description: Indicate a minimum number of eligible domains
          title: minDomains
          type: integer
        nodeAffinityPolicy:
          default: Honor
          description: Indicate how we will treat Pod's nodeAffinity/nodeSelector
            when calculating pod topology spread skew
          enum:
          - Honor
          - Ignore
          title: nodeAffinityPolicy
          type: string
        nodeTaintsPolicy:
          default: Honor
          description: Indicate how we will treat node taints when calculating pod
            topology spread skew
          enum:
          - Honor
          - Ignore
          title: nodeTaintsPolicy
          type: string
        topologyKey:
          description: Specify the key of node labels
          title: topologyKey
          type: string
        whenUnsatisfiable:
          default: DoNotSchedule
          description: Indicate how to deal with a Pod if it doesn't satisfy the spread
            constraint
          enum:
          - DoNotSchedule
          - ScheduleAnyway
          title: whenUnsatisfiable
          type: string
      required:
      - labelSelector
      - maxSkew
      - topologyKey
      - whenUnsatisfiable
      title: constraints
      type: object


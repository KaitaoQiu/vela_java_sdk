openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    virtualgroup-spec:
      properties:
        group:
          default: default
          title: group
          type: string
        type:
          default: namespace
          title: type
          type: string
      required:
      - group
      - type
      title: parameter
      type: object


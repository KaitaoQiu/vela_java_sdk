openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    apply-deployment-spec:
      properties:
        cluster:
          default: ""
          title: cluster
          type: string
        cmd:
          items:
            title: cmd
            type: string
          title: cmd
          type: array
        image:
          title: image
          type: string
        replicas:
          default: 1
          title: replicas
          type: integer
      required:
      - cluster
      - image
      - replicas
      title: parameter
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    check-metrics-spec:
      properties:
        condition:
          description: "Condition is an expression which determines if a measurement\
            \ is considered successful. eg: >=0.95"
          title: condition
          type: string
        duration:
          default: 5m
          description: Duration defines the duration of time required for this step
            to be considered successful.
          title: duration
          type: string
        failDuration:
          default: 2m
          description: "FailDuration is the duration of time that, if the check fails,\
            \ will result in the step being marked as failed."
          title: failDuration
          type: string
        metricEndpoint:
          $ref: '#/components/schemas/metricEndpoint'
        query:
          description: Query is a raw prometheus query to perform
          title: query
          type: string
      required:
      - condition
      - query
      title: parameter
      type: object
    metricEndpoint:
      description: The HTTP address and port of the prometheus server
      oneOf:
      - enum:
        - http://prometheus-server.o11y-system.svc:9090
        type: string
      title: metricEndpoint
      type: string


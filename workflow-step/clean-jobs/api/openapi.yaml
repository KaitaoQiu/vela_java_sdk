openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    clean-jobs-spec:
      properties:
        labelselector:
          title: labelselector
          type: object
        namespace:
          title: namespace
          type: string
      required:
      - namespace
      title: parameter
      type: object


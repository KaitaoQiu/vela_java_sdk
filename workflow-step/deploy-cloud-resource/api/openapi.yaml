openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    deploy-cloud-resource-spec:
      properties:
        env:
          description: Declare the name of the env in policy
          title: env
          type: string
        policy:
          default: ""
          description: "Declare the name of the env-binding policy, if empty, the\
            \ first env-binding policy will be used"
          title: policy
          type: string
      required:
      - env
      - policy
      title: parameter
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    deploy-spec:
      properties:
        auto:
          default: true
          description: "If set to false, the workflow will suspend automatically before\
            \ this step, default to be true."
          title: auto
          type: boolean
        ignoreTerraformComponent:
          default: true
          description: "If set false, this step will apply the components with the\
            \ terraform workload."
          title: ignoreTerraformComponent
          type: boolean
        parallelism:
          default: 5
          description: Maximum number of concurrent delivered components.
          title: parallelism
          type: integer
        policies:
          description: "Declare the policies that used for this deployment. If not\
            \ specified, the components will be deployed to the hub cluster."
          items:
            title: policies
            type: string
          title: policies
          type: array
      required:
      - auto
      - ignoreTerraformComponent
      - parallelism
      - policies
      title: parameter
      type: object


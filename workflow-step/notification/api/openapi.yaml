openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    block:
      properties:
        block_id:
          title: block_id
          type: string
        elements:
          items:
            $ref: '#/components/schemas/elements'
          title: elements
          type: array
        type:
          title: type
          type: string
      required:
      - type
      title: blocks
      type: object
    link:
      properties:
        messageUrl:
          title: messageUrl
          type: string
        picUrl:
          title: picUrl
          type: string
        text:
          title: text
          type: string
        title:
          title: title
          type: string
      title: links
      type: object
    notification-spec:
      properties:
        dingding:
          $ref: '#/components/schemas/dingding'
        email:
          $ref: '#/components/schemas/email'
        lark:
          $ref: '#/components/schemas/lark'
        slack:
          $ref: '#/components/schemas/slack'
      title: parameter
      type: object
    option:
      properties:
        description:
          $ref: '#/components/schemas/textType'
        text:
          $ref: '#/components/schemas/textType'
        url:
          title: url
          type: string
        value:
          title: value
          type: string
      required:
      - text
      - value
      title: option_groups
      type: object
    textType:
      properties:
        emoji:
          title: emoji
          type: boolean
        text:
          title: text
          type: string
        type:
          title: type
          type: string
        verbatim:
          title: verbatim
          type: boolean
      required:
      - text
      - type
      title: textType
      type: object
    confirm:
      properties:
        confirm:
          $ref: '#/components/schemas/textType'
        deny:
          $ref: '#/components/schemas/textType'
        style:
          title: style
          type: string
        text:
          $ref: '#/components/schemas/textType'
        title:
          $ref: '#/components/schemas/textType'
      required:
      - confirm
      - deny
      - text
      - title
      title: confirm
      type: object
    dispatch_action_config:
      properties:
        trigger_actions_on:
          items:
            title: trigger_actions_on
            type: string
          title: trigger_actions_on
          type: array
      title: dispatch_action_config
      type: object
    elements:
      properties:
        action_id:
          title: action_id
          type: string
        alt_text:
          title: alt_text
          type: string
        confirm:
          $ref: '#/components/schemas/confirm'
        dispatch_action_config:
          $ref: '#/components/schemas/dispatch_action_config'
        image_url:
          title: image_url
          type: string
        initial_date:
          title: initial_date
          type: string
        initial_options:
          items:
            $ref: '#/components/schemas/option'
          title: initial_options
          type: array
        initial_time:
          title: initial_time
          type: string
        initial_value:
          title: initial_value
          type: string
        max_length:
          title: max_length
          type: integer
        max_selected_items:
          title: max_selected_items
          type: integer
        min_length:
          title: min_length
          type: integer
        multiline:
          title: multiline
          type: boolean
        option_groups:
          items:
            $ref: '#/components/schemas/option'
          title: option_groups
          type: array
        options:
          items:
            $ref: '#/components/schemas/option'
          title: options
          type: array
        placeholder:
          $ref: '#/components/schemas/textType'
        style:
          title: style
          type: string
        text:
          $ref: '#/components/schemas/textType'
        type:
          title: type
          type: string
        url:
          title: url
          type: string
        value:
          title: value
          type: string
      required:
      - type
      title: elements
      type: object
    btns:
      properties:
        actionURL:
          title: actionURL
          type: string
        title:
          title: title
          type: string
      required:
      - actionURL
      - title
      title: btns
      type: object
    actionCard:
      properties:
        btnOrientation:
          title: btnOrientation
          type: string
        btns:
          items:
            $ref: '#/components/schemas/btns'
          title: btns
          type: array
        hideAvatar:
          title: hideAvatar
          type: string
        singleTitle:
          title: singleTitle
          type: string
        singleURL:
          title: singleURL
          type: string
        text:
          title: text
          type: string
        title:
          title: title
          type: string
      required:
      - btnOrientation
      - hideAvatar
      - singleTitle
      - singleURL
      - text
      - title
      title: actionCard
      type: object
    at:
      properties:
        atMobiles:
          items:
            title: atMobiles
            type: string
          title: atMobiles
          type: array
        isAtAll:
          title: isAtAll
          type: boolean
      title: at
      type: object
    feedCard:
      properties:
        links:
          items:
            $ref: '#/components/schemas/link'
          title: links
          type: array
      required:
      - links
      title: feedCard
      type: object
    markdown:
      properties:
        text:
          title: text
          type: string
        title:
          title: title
          type: string
      required:
      - text
      - title
      title: markdown
      type: object
    text:
      description: Specify the message content of dingtalk notification
      properties:
        content:
          title: content
          type: string
      required:
      - content
      title: text
      type: object
    message:
      description: "Specify the message that you want to sent, refer to [dingtalk\
        \ messaging](https://developers.dingtalk.com/document/robots/custom-robot-access/title-72m-8ag-pqw)"
      properties:
        actionCard:
          $ref: '#/components/schemas/actionCard'
        at:
          $ref: '#/components/schemas/at'
        feedCard:
          $ref: '#/components/schemas/feedCard'
        link:
          $ref: '#/components/schemas/link'
        markdown:
          $ref: '#/components/schemas/markdown'
        msgtype:
          default: text
          description: "msgType can be text, link, mardown, actionCard, feedCard"
          enum:
          - text
          - link
          - markdown
          - actionCard
          - feedCard
          title: msgtype
          type: string
        text:
          $ref: '#/components/schemas/text'
      required:
      - msgtype
      title: message
      type: object
    url_oneOf:
      properties:
        value:
          description: the url address content in string
          title: value
          type: string
      required:
      - value
      type: object
    secretRef:
      properties:
        key:
          description: key is the key in the secret
          title: key
          type: string
        name:
          description: name is the name of the secret
          title: name
          type: string
      required:
      - key
      - name
      title: secretRef
      type: object
    url_oneOf_1:
      properties:
        secretRef:
          $ref: '#/components/schemas/secretRef'
      required:
      - secretRef
      type: object
    url:
      description: "Specify the the dingding url, you can either sepcify it in value\
        \ or use secretRef"
      oneOf:
      - $ref: '#/components/schemas/url_oneOf'
      - $ref: '#/components/schemas/url_oneOf_1'
      title: url
      type: object
    dingding:
      description: Please fulfill its url and message if you want to send DingTalk
        messages
      properties:
        message:
          $ref: '#/components/schemas/message'
        url:
          $ref: '#/components/schemas/url'
      required:
      - message
      - url
      title: dingding
      type: object
    content:
      description: Specify the content of the email
      properties:
        body:
          description: Specify the context body of the email
          title: body
          type: string
        subject:
          description: Specify the subject of the email
          title: subject
          type: string
      required:
      - body
      - subject
      title: content
      type: object
    password_oneOf:
      properties:
        value:
          description: the password content in string
          title: value
          type: string
      required:
      - value
      type: object
    password:
      description: "Specify the password of the email, you can either sepcify it in\
        \ value or use secretRef"
      oneOf:
      - $ref: '#/components/schemas/password_oneOf'
      - $ref: '#/components/schemas/url_oneOf_1'
      title: password
      type: object
    from:
      description: Specify the email info that you want to send from
      properties:
        address:
          description: Specify the email address that you want to send from
          title: address
          type: string
        alias:
          description: The alias is the email alias to show after sending the email
          title: alias
          type: string
        host:
          description: Specify the host of your email
          title: host
          type: string
        password:
          $ref: '#/components/schemas/password'
        port:
          default: 587
          description: "Specify the port of the email host, default to 587"
          title: port
          type: integer
      required:
      - address
      - host
      - password
      - port
      title: from
      type: object
    email:
      description: "Please fulfill its from, to and content if you want to send email"
      properties:
        content:
          $ref: '#/components/schemas/content'
        from:
          $ref: '#/components/schemas/from'
        to:
          description: Specify the email address that you want to send to
          items:
            title: to
            type: string
          title: to
          type: array
      required:
      - content
      - from
      - to
      title: email
      type: object
    message_1:
      description: "Specify the message that you want to sent, refer to [Lark messaging](https://open.feishu.cn/document/ukTMukTMukTM/ucTM5YjL3ETO24yNxkjN#8b0f2a1b)."
      properties:
        content:
          description: content should be json encode string
          title: content
          type: string
        msg_type:
          description: "msg_type can be text, post, image, interactive, share_chat,\
            \ share_user, audio, media, file, sticker"
          title: msg_type
          type: string
      required:
      - content
      - msg_type
      title: message
      type: object
    url_1:
      description: "Specify the the lark url, you can either sepcify it in value or\
        \ use secretRef"
      oneOf:
      - $ref: '#/components/schemas/url_oneOf'
      - $ref: '#/components/schemas/url_oneOf_1'
      title: url
      type: object
    lark:
      description: Please fulfill its url and message if you want to send Lark messages
      properties:
        message:
          $ref: '#/components/schemas/message_1'
        url:
          $ref: '#/components/schemas/url_1'
      required:
      - message
      - url
      title: lark
      type: object
    attachments:
      properties:
        blocks:
          items:
            $ref: '#/components/schemas/block'
          title: blocks
          type: array
        color:
          title: color
          type: string
      title: attachments
      type: object
    message_2:
      description: "Specify the message that you want to sent, refer to [slack messaging](https://api.slack.com/reference/messaging/payload)"
      properties:
        attachments:
          $ref: '#/components/schemas/attachments'
        blocks:
          items:
            $ref: '#/components/schemas/block'
          title: blocks
          type: array
        mrkdwn:
          default: true
          description: Specify the message text format in markdown for slack notification
          title: mrkdwn
          type: boolean
        text:
          description: Specify the message text for slack notification
          title: text
          type: string
        thread_ts:
          title: thread_ts
          type: string
      required:
      - text
      title: message
      type: object
    url_2:
      description: "Specify the the slack url, you can either sepcify it in value\
        \ or use secretRef"
      oneOf:
      - $ref: '#/components/schemas/url_oneOf'
      - $ref: '#/components/schemas/url_oneOf_1'
      title: url
      type: object
    slack:
      description: Please fulfill its url and message if you want to send Slack messages
      properties:
        message:
          $ref: '#/components/schemas/message_2'
        url:
          $ref: '#/components/schemas/url_2'
      required:
      - message
      - url
      title: slack
      type: object


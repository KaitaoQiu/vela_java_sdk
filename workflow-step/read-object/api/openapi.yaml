openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    read-object-spec:
      properties:
        apiVersion:
          description: "Specify the apiVersion of the object, defaults to 'core.oam.dev/v1beta1'"
          title: apiVersion
          type: string
        cluster:
          default: ""
          description: "The cluster you want to apply the resource to, default is\
            \ the current control plane cluster"
          title: cluster
          type: string
        kind:
          description: "Specify the kind of the object, defaults to Application"
          title: kind
          type: string
        name:
          description: Specify the name of the object
          title: name
          type: string
        namespace:
          default: default
          description: The namespace of the resource you want to read
          title: namespace
          type: string
      required:
      - cluster
      - name
      title: parameter
      type: object


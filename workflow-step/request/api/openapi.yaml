openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    request-spec:
      properties:
        body:
          title: body
          type: object
        header:
          additionalProperties:
            type: string
          title: header
          type: object
        method:
          default: GET
          enum:
          - GET
          - POST
          - PUT
          - DELETE
          title: method
          type: string
        url:
          title: url
          type: string
      required:
      - method
      - url
      title: parameter
      type: object


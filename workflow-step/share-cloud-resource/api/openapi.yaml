openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    share-cloud-resource-spec:
      properties:
        env:
          description: Declare the name of the env in policy
          title: env
          type: string
        placements:
          description: Declare the location to bind
          items:
            $ref: '#/components/schemas/placements'
          title: placements
          type: array
        policy:
          default: ""
          description: "Declare the name of the env-binding policy, if empty, the\
            \ first env-binding policy will be used"
          title: policy
          type: string
      required:
      - env
      - placements
      - policy
      title: parameter
      type: object
    placements:
      properties:
        cluster:
          title: cluster
          type: string
        namespace:
          title: namespace
          type: string
      title: placements
      type: object


openapi: 3.0.0
info:
  title: Generated by cue.
  version: no version
servers:
- url: /
paths: {}
components:
  schemas:
    webhook-spec:
      properties:
        data:
          description: Specify the data you want to send
          title: data
          type: object
        url:
          $ref: '#/components/schemas/url'
      required:
      - url
      title: parameter
      type: object
    url_oneOf:
      properties:
        value:
          title: value
          type: string
      required:
      - value
      type: object
    secretRef:
      properties:
        key:
          description: key is the key in the secret
          title: key
          type: string
        name:
          description: name is the name of the secret
          title: name
          type: string
      required:
      - key
      - name
      title: secretRef
      type: object
    url_oneOf_1:
      properties:
        secretRef:
          $ref: '#/components/schemas/secretRef'
      required:
      - secretRef
      type: object
    url:
      description: Specify the webhook url
      oneOf:
      - $ref: '#/components/schemas/url_oneOf'
      - $ref: '#/components/schemas/url_oneOf_1'
      title: url
      type: object

